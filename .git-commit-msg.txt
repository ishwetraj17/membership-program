Phase 14: Reconciliation mismatch taxonomy, expectation classification, timing-window handling

- V60 migration: recon_mismatches adds expectation/severity/gateway_transaction_id/merchant_id/currency/settlement_currency/fx_rate; payment_intents_v2 adds settlement_currency/settlement_amount_minor/fx_rate/fx_rate_captured_at
- ReconExpectation enum: EXPECTED_TIMING_DIFFERENCE, EXPECTED_RISK_HOLD, EXPECTED_DISPUTE_HOLD, UNEXPECTED_SYSTEM_ERROR, UNEXPECTED_GATEWAY_ERROR
- ReconSeverity enum: INFO, WARNING, CRITICAL
- ReconWindowPolicy: configurable slack window (default 30 min) around day boundaries; recon.window.minutes property
- ReconExpectationClassifier: stateless classifier — MismatchType + nearBoundary flag -> expectation/severity pair
- OrphanGatewayPaymentChecker: finds SUCCEEDED attempts with gateway_transaction_id but no invoice (gateway is reconciliation anchor, not invoice alone); creates ORPHAN_GATEWAY_PAYMENT/CRITICAL mismatches
- DuplicateSettlementChecker: GROUP BY merchant_id/batch_date HAVING COUNT > 1; creates DUPLICATE_SETTLEMENT/CRITICAL mismatches
- MismatchType: added ORPHAN_GATEWAY_PAYMENT, DUPLICATE_SETTLEMENT
- ReconMismatch + ReconMismatchDTO: 7 new fields (expectation, severity, gatewayTransactionId, merchantId, currency, settlementCurrency, fxRate)
- PaymentIntentV2: 4 new FX/settlement fields
- AdvancedReconciliationService: runForDateWithWindow(), listOrphanedGatewayPayments()
- ReconMismatchAdminController: base path /api/v2/admin/recon; POST /run, GET /orphaned-gateway-payments
- Phase14ReconTaxonomyTests: 15 tests — 5 window policy, 6 classifier, 2 orphan checker, 2 duplicate settlement checker
- docs/operations/01-reconciliation-playbook.md: Phase 14 section with taxonomy table, timing-window config, new mismatch types SQL, API examples, escalation ladder

Tests: 1804 run, 0 failures, 0 errors (280 skipped Testcontainers)
