Phase 15: Revenue recognition guard rules and deferred revenue correctness

V61 migration: adds expected_amount_minor, recognized_amount_minor,
rounding_adjustment_minor, policy_code, guard_decision, guard_reason
to revenue_recognition_schedules with partial indexes.

RevenueRecognitionGuard: stateless rule evaluator mapping subscription
and invoice state to GuardDecision (ALLOW/FLAG/DEFER/BLOCK/HALT) and
RecognitionPolicyCode (RECOGNIZE/SKIP/DEFER_UNTIL_PAID/HALT/REVERSE_ON_VOID).

RevenueScheduleAllocator: replaces private buildDailySchedule, adds
minor-unit tracking (expectedAmountMinor, roundingAdjustmentMinor)
on every schedule row.

RevenueCatchUpService + RevenueCatchUpServiceImpl: batch catch-up
orchestrator evaluates guard per PENDING row, stamps decision via
RevenueCatchUpTransactionHelper (REQUIRES_NEW), then posts via
existing postSingleRecognitionInRun (REQUIRES_NEW).

RevenueCatchUpTransactionHelper: REQUIRES_NEW helpers (stampAndSkip,
stampDefer, stampAllowAndMarkCatchUp) ensure guard fields are committed
before the posting service reloads the entity.

RevenueRecognitionDriftChecker + DriftCheckResult: amount drift and
timing drift (overdue PENDING rows) detection per invoice.

Modified: RevenueRecognitionStatus (SKIPPED), RevenueRecognitionSchedule
(6 new fields), RevenueRecognitionScheduleResponseDTO, repository
(countOverduePendingByInvoiceId, findByInvoiceIdOrderByRecognitionDateAsc),
RevenueRecognitionPostingServiceImpl (recognizedAmountMinor on POSTED),
RevenueRecognitionScheduleServiceImpl (allocator + toDto),
RevenueAdminController (POST /post-due, POST /rebuild/{invoiceId}).

Phase15RevenueRecognitionTests: 24 new tests across allocator, guard,
catch-up service, drift checker, posting minor-unit stamping.

docs/accounting/02-revenue-recognition.md: Phase 15 section.

1828 tests, 0 failures, BUILD SUCCESS
